apiVersion: apps/v1
kind: Deployment
metadata:
  name: delay-dashboard
  labels:
    app: delay-dashboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: delay-dashboard
  template:
    metadata:
      labels:
        app: delay-dashboard
    spec:
      containers:
      - name: delay-dashboard
        image: ghcr.io/drasi-project/learning/curbside-pickup/delay-dashboard:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          name: http
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: delay-dashboard
  labels:
    app: delay-dashboard
spec:
  type: ClusterIP
  selector:
    app: delay-dashboard
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: delay-dashboard-stripprefix
spec:
  stripPrefix:
    prefixes:
    - /delay-dashboard
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: delay-dashboard
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: default-delay-dashboard-stripprefix@kubernetescrd
spec:
  rules:
  - http:
      paths:
      - path: /delay-dashboard
        pathType: Prefix
        backend:
          service:
            name: delay-dashboard
            port:
              number: 80