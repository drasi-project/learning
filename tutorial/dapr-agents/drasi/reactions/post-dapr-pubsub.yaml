kind: Reaction
apiVersion: v1
name: stock-notifications-publisher
spec:
  kind: PostDaprPubSub
  queries:
    # Publish low stock events to the order agent's topic
    low-stock-event-query: >
      {
        "pubsubName": "notifications-pubsub",
        "topicName": "order-agent-topic",
        "format": "Unpacked",
        "skipControlSignals": true
      }
    
    # Publish critical stock events to the order agent's topic
    critical-stock-event-query: >
      {
        "pubsubName": "notifications-pubsub",
        "topicName": "order-agent-topic",
        "format": "Unpacked",
        "skipControlSignals": true
      }